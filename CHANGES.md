# 变更记录

## 用户侧验证码可靠性 A 阶段（本地未发布）

- GT3/GT4 人工验证统一处理关闭、初始化/人工/提交超时、缺字段、SDK 错误和重复/迟到回调；保留官方验证与仅业务码 -1100 的降级规则。
- 游戏挑战按账号串行，与托管操作共享展示锁；新挑战、账号删除、退出/切换身份使旧任务失效。同一失败挑战不因轮询反复弹出。
- Dashboard 和游戏详情展示验证进度与独立服务器状态，提供取消、重新读取和主动重试。提交接受不等于登录成功；取消不代表服务器撤销。
- GET 请求倒序保护、有限状态确认、请求级 timeout/signal；保留改密码删除仅一次的回归。
- 本地合成验证：独立 PR 分支基于上游 master，172 项 Jest、类型、变更文件 lint、构建和完整页面模拟通过；375/768/1440 与横屏、键盘焦点检查通过。未调用真实验证码或账号。此前含基建的开发分支为 306 项测试，两者不混用验收数量。
- 限制：没有服务端登录尝试 ID，不能证明单次验证与运行状态的因果关联。GT4 destroy 见官方 API；GT3 文档未明确 destroy，保留现有对象可选销毁能力，真实 SDK 清理/可访问性仍需发布前验证。
- 不含基建排班功能、工作台可靠性、识别器、采集或模型训练；公告 A 阶段在另一功能分支，未合并、未部署。
